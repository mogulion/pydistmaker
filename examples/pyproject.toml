[tool.poetry]
name = "examples"
version = "0.1.0"
description = ""
authors = ["yang, qingfeng <qingfeng.yang@iav.de>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.11,<3.14"
pydistmaker = {version = "^0.1.0", source = "pypi-iav-cn"}

[[tool.poetry.source]]
name = "pypi-iav-cn"
url = "https://artifactory.iav.com/artifactory/api/pypi/pypi-iav-cn/simple/"
priority = "explicit"  # 可选值: 'default', 'primary', 'supplemental', 'explicit'.

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
